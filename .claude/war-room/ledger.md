# War-room ledger

Source of truth for the war-room backlog across loop iterations. The chair
updates it after every Round D ruling and every verified fix. One row per item;
never delete a row — move it to Closed or Declined with the evidence.

Status: `open` · `in-progress (<branch>)` · `fixed (<branch/PR>)` ·
`verified (<PR>, CI run)` · `blocked (<what is needed>)`.
Evidence tier as in `repo-map.md`.

## Iteration log

| # | Date | Scope | Seats | Outcome |
|---|---|---|---|---|
| 0 | 2026-09-23 | Whole-codebase audit (pre-war-room: 5 area auditors + Fable ranking) | identity, commerce, payments, tests, ops auditors | Seeded this ledger; 5 fix-now items dispatched |
| 1 | 2026-09-23 | Fix-now wave A-1…A-5 (3 worktree implementers) | payments, ops, identity implementers | A-1…A-5 fixed on war-room/integration; full local gate green; A-21 added |
| 2 | 2026-09-23 | QA verification of A-1…A-5 | opsec-qa-release | A-1…A-5 VERIFIED (CI 35896554121); QA-1 concurrent password change on revoked session (SHOULD, reproduced) fixed + regression test; QA-2/QA-3 doc and restore-name NITs fixed; A-22 added |
| 3 | 2026-09-23 | Wave A-6/A-7/A-21 + whole-product completeness & evidence sweep (Rounds 0–D, B+C combined) | product-completeness, qa-release, devils-advocate, staff; 3 implementers | A-6, A-7, A-21 fixed on war-room/integration; QA ran chain-journey (BTC+XMR) + regtest-smoke on 509356d (pass); new P0 A-23, P1 A-24…A-30, P2 A-31…A-41, P3 A-42…A-45; A-14/A-19 declined; A-12 → P2 |
| 3b | 2026-09-23 | Bug hunt #1 (new `/bug-hunt` group: codegraph, 2 live probers, 3 static hunters, 3 verifier batches) + wave 2 (A-23, A-24, A-25) | codegraph, workflow-prober ×2, bug-hunter ×3, bug-verifier ×3, implementers ×3, qa-release | 39 raw findings → 1 P0 (A-52), 4 P1 (A-47, A-48, A-53, A-54), 13 P2/P3 rows, 1 duplicate, 7 rejected → rules learned; A-23/A-24/A-25 VERIFIED (CI 35908986249); hunt tooling fixed 3× from its own run |
| 4 | 2026-09-23 | Wave 3 (A-30, A-47, A-48, A-52 — P0 dispatched as a 4th implementer, deviation noted) | implementers ×4, qa-release | all four VERIFIED on cdb69f2 (CI 35913448670; full Playwright 336/336; real-chain rerun PASS); A-64, A-65 NITs added; bug hunt #2 started |
| 5 | 2026-09-24 | Resume after owner merged PR #7 (main 4c360d2); no council needed (ready P0/P1 ≥ 3); rule A-66/A-67/A-72; wave 4 | chair; implementers ×3, qa-release | A-66/A-67/A-72 fixed; full local gate + CI 35952354841 green on 766c87f (PR #8); QA pending; bash -n gate text fixed; A-73 added |
| 6 | 2026-09-24 | Wave 5 (A-53, A-54, A-26 + A-71), started beside iteration-5 QA; A-74 follow-up | implementers ×4, qa-release | A-66/A-67/A-72 VERIFIED; wave 5 fixed, full local gate + CI 35954364843 green on bf1ddc9, QA pending; A-74 fixed |
| 7 | 2026-09-24 | Wave 6 (A-8, A-10, A-11) with rulings written for A-8…A-11 | chair; implementers ×3, qa-release | all fixed and integrated (A-10/A-11 doc conflict resolved by the chair); CI 35956392431 green on 30be215; A-75 added; QA pending |
| 8 | 2026-09-24 | Wave 7 (A-9, A-28, A-29) | implementers ×3, qa-release | all VERIFIED on 0a1b75d (CI 35958151032; local gate incl. Playwright 336/336); A-76, A-77, A-78 added. Iterations 5–8 of this run: 14 items VERIFIED (A-66, A-67, A-72, A-53, A-54, A-26, A-71, A-74, A-8, A-10, A-11, A-9, A-28, A-29), 0 reopened; max iterations reached |
| 9 | 2026-09-24 | Resume (run 2). Sweeps: payments (payments, qa-release, staff, devils-advocate) and privacy (privacy, security, frontend, buyer), Rounds 0–D; owner asked that operations, UI and privacy not be skipped, so two sweeps per iteration | 8 seats; implementers ×7 (A-13, A-27, A-79, A-82, A-81, A-97, A-102); qa-release ×2 | VERIFIED: A-13, A-27 (CI 36000605614), A-79, A-82 (CI 36001767193). Privacy ruling A-79…A-96; payments ruling P0 A-97 + A-98…A-103; A-97, A-102, A-81 fixed on e538601 (gate + QA pending). CI 35998094632 red on a known click-then-reload flake (A-12), next run green; GitGuardian false positive on a test handle (owner dashboard) |

## P0 — release blockers

| ID | Item | Area | Evidence | Status |
|---|---|---|---|---|
| A-1 | Payout wallet calls clamped to 10 s (`payments_rpc.go:37,64-74`) under the 30 s send bound; ambiguous send recorded failed, admin requeue can double-pay | payments | code read, verified | verified (PR #6; opsec-qa-release; fix/payout-timeout 12b3e71) |
| A-2 | No working restore/rollback for the default internal-db deployment; `UPGRADING.md` rollback impossible with `build: .` | ops | code read, verified | verified (PR #6; opsec-qa-release; fix/ops-restore-setup-token 868d94f) |
| A-23 | Monero payout send classifies every JSON-RPC error as "nothing broadcast" (`walletRejected`, `payments_watcher.go:547`); monero-wallet-rpc v0.18.5.1 returns -38 (no daemon connection) when `sendrawtransaction` times out after the daemon may have relayed, and -4 from post-submit checks → requeue skips the wallet confirmation → possible double pay (DA-1) | payments | source trace (DA), code read (chair) | verified (PR #7 @f66ab6b, CI 35908986249; opsec-qa-release, v0.18.5.1 source re-checked; fix/a23-monero-ambiguous-send 866210a, migration 055) |
| A-52 | `enqueuePayout` sums settled deposits and later marks `credited=true WHERE settled` in separate statements under READ COMMITTED while `recordIncoming` upserts without the order lock: a deposit reaching the threshold in between is credited but not paid and never flagged, and a counted deposit turning conflicted in between is paid out uncredited and never held — verifier reproduced 57/400 and 80/400 without any trigger (33 payouts included a deposit the ledger already marked conflicted) (HUNT-E-2, bug hunt #1) | payments | Go DB race, verified | verified (PR #7 @cdb69f2, CI 35913448670; opsec-qa-release with mutation checks; -race count=5 15/15, real-chain payout sum = credited sum (QA-reported; log not retained — DA9-8); fix/a52-enqueue-credit-race c40769a) |
| A-66 | After the documented side-by-side restore or rollback (`DATABASE_URL` switched to `opsecmkt_restored`/`opsecmkt_rollback`), `scripts/backup.sh` still dumps hard-coded `-d opsecmkt`: every later backup silently omits all live data; no documented path backs up the live internal database (HUNT2-D-1; repro: internal-db Compose transcript, verified) | ops | ops tier (real Compose + age), verified | verified (PR #8 @766c87f, CI 35952354841; opsec-qa-release with mutation checks; restore scripts under bash 5 with real Compose + age, no credentials in logs; fix/a66-backup-live-db e70c428) |
| A-97 | Unsent payouts on closed orders are sent on a stale ledger: `pollCurrency`'s watch set (`payments_watcher.go:158-163`) drops a terminal order 24 h after its last update once its credited deposits reach the threshold, and `sendPayouts` (:508-511) claims its pending payout without a fresh read — a blocked/failed/restore-held payout released later is sent although its deposit was reversed (never flagged or held); also `sendPayouts` runs for orders whose `settleOrder` failed this pass, leaving the unpinned claim re-check (:510) as the only guard (PAY9-1, reproduced by payments and independently by QA; DA9 MISSED; converse: a watcher-held payout on an address >30 days old never un-holds) | payments | Go DB + fake provider, reproduced twice | QA-verified on 483d944, CI rerun pending — not yet closed (opsec-qa-release: pre-fix repro on 25432f4 fails / passes after, 21 mutations (20 killed; M15 has no observable effect), interplay with restore/suspension holds, Playwright 390 + wallet 4/4; CI 36006118748 red only on the A-12 race — rerun pending; fix/a97-send-after-fresh-read ffbc475) |
| A-3 | `.env.example` placeholder `SETUP_TOKEN` passes the length-only check (`app.go:78`); token also derives CSRF and TOTP sealing keys | ops/identity | code read, verified | verified (PR #6; opsec-qa-release; fix/ops-restore-setup-token 868d94f) |

## P1 — required for a coherent product

| ID | Item | Area | Evidence | Status |
|---|---|---|---|---|
| A-4 | Role-change audit row lacks target/role (`actions_admin.go:32`); admin audit view lacks actor (`load.go:206`) | identity | code read, verified | verified (PR #6; opsec-qa-release; fix/identity-audit-credentials 351d6b9) |
| A-5 | No password change; no admin reset of a user's second factors (lost TOTP + spent codes = SQL-only) | identity | code read, verified | verified (PR #6; opsec-qa-release; fix/identity-audit-credentials 351d6b9) |
| A-6 | Moderator desk mixes open+resolved disputes `ORDER BY created DESC LIMIT 100` (`load.go:163`); old open disputes vanish; same cap feeds NeedsAction on orders/vendor desks | commerce | code read, verified | verified (PR #7 @699ea45, CI 35905170138; opsec-qa-release with mutation checks; fix/a6-open-disputes-first 9b145fa) |
| A-7 | Watcher re-polls funded non-terminal orders only if updated in 24 h (`payments_watcher.go:147-151`); extra/duplicate deposits on paid/shipped orders are silently included in the vendor release (`payments_hooks.go:71-73`) | payments | auditor + Fable read | verified (PR #7 @699ea45, CI 35905170138; opsec-qa-release with mutation checks + real-chain journey BTC/XMR and regtest smoke on 699ea45 (QA-reported; log not retained — DA9-8); fix/a7-watch-funded-orders fffc2a8) |
| A-8 | Moderators/admins not notified when a dispute opens; a dispute can have no eligible resolver (sole admin is the vendor) yet is accepted | commerce | auditor read | verified (PR #8 @30be215, CI 35956392431; opsec-qa-release with mutation checks; preview-orders/contacts 54/54, full Playwright 336/336; fix/a8-dispute-staff-notify 75c3a5d) |
| A-9 | One 15 s startup context covers ping, advisory lock, all migrations and payment init (`app.go:95-115`); a slow migration crash-loops | ops | auditor read | verified (PR #8 @0a1b75d, CI 35958151032; opsec-qa-release with mutation checks; real process as non-superuser, alpha.1 upgrade; fix/a9-startup-phase-timeouts 6cc0bee) |
| A-10 | Sealed plaintext recovery codes linger in `users.recovery_reveal` unless `/totp` is viewed within 10 min; docs claim only hashes are stored | identity | auditor + Fable read | verified (PR #8 @30be215, CI 35956392431; opsec-qa-release with mutation checks; live start/stop run; fix/a10-recovery-reveal-sweep 1411c98) |
| A-11 | Rate-limit entry created before CAPTCHA check + evict-oldest lets junk requests reset victims' counters (`actions_auth.go:128-131`, `app.go:148`) | identity | auditor read | verified (PR #8 @30be215, CI 35956392431; opsec-qa-release with mutation checks; fix/a11-limiter-order 8f076f0; doc merge checked) |
| A-21 | After a restore, in-flight payouts become `held`; admin **release** of a held payout needs no "wallet shows no broadcast" confirmation, unlike requeue of an ambiguous failure (reported by the A-1 implementer) | payments | implementer read | verified (PR #7 @699ea45, CI 35905170138; opsec-qa-release with mutation checks; fix/a21-restore-held-release 62a3871) |
| A-13 | Docs drift: acceptance.md ops/P5 unticked despite evidence; implementation-status says regtest smoke never ran; `.ralph/fix_plan.md` ticks a non-existent `.env` recovery doc; PREVIEW_ADDR undocumented; APP_MODE is a no-op; Caddy example hardcodes :8080 | ops | auditor read | verified (PR #9 @08a9437, CI 36000605614; opsec-qa-release: 11 CI rows re-checked with gh, 62 cited tests exist, 12 spot-checked; fix/a13-docs-evidence f9209c3) |
| A-24 | Payment-review flags lead nowhere: staff notified by `flagOrder` get 404 on the order (`disputeReviewer` allows only disputed/resolved) and /moderator lists only disputes (PM-1, MOD-1, DA-4, QA3-1 — reproduced by QA) | payments/staff | Go DB probe (QA, scratch) | verified (PR #7 @f66ab6b, CI 35908986249; opsec-qa-release with mutation checks; fix/a24-flag-review f432182 + e1bfcd9) |
| A-25 | Admin payouts panel `ORDER BY p.id DESC LIMIT 50` (`payments_actions.go:279`): an older failed/held/stuck payout and its only recovery form drop off the page (MOD-3) | payments/staff | code read | verified (PR #7 @f66ab6b, CI 35908986249; opsec-qa-release with mutation checks; fix/a25-payout-attention 675ffc6 + e1bfcd9 + f66ab6b) |
| A-26 | Role form is a `<select>` over the 100 newest users (`load.go:182`): older vendors/moderators cannot be demoted without SQL; ~100 sign-ups push them out (DA-3, MOD-5) | identity/staff | code read | verified (PR #8 @bf1ddc9, CI 35954364843; opsec-qa-release with mutation checks; Render hook safety incl. CSRF, escaping, headers; full Playwright 336/336; fix/a26-handle-lookup 32d2f9c) |
| A-27 | No way to contain a compromised or abusive account: admins can end another user's sessions only via factor reset (409 without a factor); nothing blocks sign-in (PM-2, MOD-9) | identity/staff | code read | verified (PR #9 @08a9437, CI 36000605614; opsec-qa-release with 13 mutations (12 caught; FOR SHARE pin → A-108), 30× sign-in/password-change races, CSRF probes, real upgrade 060→011 and downgrade refusal; fix/a27-account-suspension 4c28e6a) |
| A-28 | Administrator password break-glass is not executable: operator-guide says "a new bcrypt hash written with SQL" with no tool or command (PM-3) | ops | code/doc read | verified (PR #8 @0a1b75d, CI 35958151032; opsec-qa-release with mutation checks; real image + Compose + Linux TTY; fix/a28-admin-password-cli 38e6205) |
| A-29 | Restore reconciliation asks the operator to reconstruct payouts sent after the backup with no statements, and contradicts itself (stop the app vs use the admin page) (PM-4) | ops/payments | doc read | verified (PR #8 @0a1b75d, CI 35958151032; opsec-qa-release with mutation checks; runbook SQL via real handler/watcher, 0 duplicate sends; internal-db rehearsal; fix/a29-restore-reconcile-runbook 466b0f8) |
| A-30 | No downgrade guard: an older binary starts silently on a database with an unknown applied migration, contradicting `UPGRADING.md:124` (QA3-2 — reproduced) | ops | real server + PostgreSQL (QA) | verified (PR #7 @cdb69f2, CI 35913448670; opsec-qa-release with mutation checks; test-upgrade.sh rollback rehearsal; fix/a30-downgrade-guard 8827f91) |
| A-47 | Adding a second factor needs only the session: PGP key change + `/pgp/2fa` enable (confirmed only for disable, `pgp_actions.go:257`) and `/totp/enroll`+`/activate` on a PGP-sign-in account; `/challenge` accepts any enrolled factor, so an attacker with a session (and the password) adds a factor that survives session revocation (HUNT-D-1, bug hunt #1) | identity | Go DB repro, verified (opsec-bug-verifier) | verified (PR #7 @cdb69f2, CI 35913448670; opsec-qa-release with mutation checks; fix/a47-factor-add-step-up 3884032) |
| A-48 | A cross-site link to the market signs the user out: a cookieless (SameSite=Strict withheld) request gets a fresh anonymous `session` cookie that overwrites the real one (`app.go:196-214`) (HUNT-D-2, bug hunt #1) | identity/ui | Go DB + real Chromium & Firefox, verified | verified (PR #7 @cdb69f2, CI 35913448670; opsec-qa-release with mutation checks; real Chromium cross-site spec; fix/a48-anon-cookie 69e57c2 + 5e811fa) |
| A-53 | Restore hold SQL skips `failed` payouts: a backed-up definite failure that was requeued and sent after the backup is restored as "nothing broadcast" and requeues with the password alone → second send once the gate clears (HUNT-E-1; widens A-21) | payments/ops | Go DB, verified | verified (PR #8 @bf1ddc9, CI 35954364843; opsec-qa-release with mutation checks; restore scripts under bash 5 + UPGRADING SQL run verbatim + end-to-end single send; fix/a53-restore-failed-payouts ae6a49a) |
| A-54 | A-7's notice promises a post-funding deposit at the threshold is included, but `enqueuePayout` with an unavailable (configured) provider counts only credited rows; fix in code using the configured provider's threshold, not a 409 (HUNT-E-3) | payments | Go DB, verified | verified (PR #8 @bf1ddc9, CI 35954364843; opsec-qa-release with mutation checks; A-52 race tests 9/9; fix/a54-unavailable-provider-threshold c40a224) |
| A-67 | /messages accepts plaintext inside the pasted armor (a `Comment:` header, a leading fake BEGIN block, lines inside the base64) and badges it green "Encrypted (to recipient's key)", contradicting acceptance.md:77 / implementation-status.md:40 ("fake armor rejected … never 'Encrypted' for plaintext"); fix must **store** a canonical re-armor of the decoded first block (headers dropped) or refuse anything after its END line — reading the packet stream to EOF alone does not close the "sandwich" variant (HUNT2-C-1 + prober A2-1 `repro-F1-message-sandwich.sh`: block + plaintext + block, or block + plaintext + bare END; both verified; `TestHuntMessagePlaintextInsideArmorBadgedEncrypted`) | messaging | Go DB, verified | verified (PR #8 @766c87f, CI 35952354841; opsec-qa-release with mutation checks; real gpg 2.2.41 message via live server; legacy header rows → A-74; fix/a67-message-armor-strict 370c726) |
| A-72 | A non-party administrator reads (and can replace) any listing's automatic-delivery secret at /listing-edit at any time — every buyer of that listing receives it, and a replacement is delivered under the vendor's name with only a generic audit row — contradicting acceptance.md:85, implementation-status.md:52,56, operator-guide.md:86 and the A-24 ruling; fix must hide on load, show "set by the vendor", and keep the stored content on admin saves (else they wipe it) (B2-1, verified: `artifacts/bug-hunt/cdb69f2/verify/batch-c/verify_b2c_test.go`) | commerce/privacy | Go DB, verified | verified (PR #8 @766c87f, CI 35952354841; opsec-qa-release with mutation checks; secret absent on every admin page, live server; fix/a72-admin-delivery-hidden c9afcfd) |
| A-79 | Saved profile PGP key is stored and republished verbatim — text around the block, armor `Comment:`/`Version:` headers (tool/OS fingerprint) — to anonymous visitors on /vendor and to any signed-in account via `/messages?to=<handle>`; /account never shows the key's user IDs (often real name + email) or who can see them; "Ownership verified" shown to others to the minute (PRIV9-2 = SEC9-1 = FE9-2 = BUY9-2, + FE9-5; DB probes by privacy, security, frontend, buyer) | identity/privacy | Go DB + live server, verified by 4 seats | verified (PR #9 @d296560, CI 36001767193; opsec-qa-release: real gpg 2.2.41 live journey 61/61, packets byte-identical, 12/13 mutations killed (fingerprint-only compare → A-111), photo IDs → A-110; fix/a79-profile-key-canonical 3695a4c) |
| A-80 | Plaintext free-text fields invite addresses: vendor "Shipping note for the buyer" (tracking numbers), cancel reason, both dispute-reason forms and the review body are stored unencrypted forever, shown to the counterparty and to staff on disputed/flagged orders, with no warning — beside "This market never stores shipping addresses"; review help says "without your handle" though the vendor sees it with the handle (PRIV9-3 = SEC9-2 = FE9-3 = BUY9-1, + BUY9-3 = PRIV9-11; buyer probe stored a plaintext address in a dispute) | commerce/privacy | Go DB probe (buyer) | in-progress (fix/a80-plaintext-field-warnings) |
| A-81 | The product never says what it records, who can read it or for how long (nothing is deletable; everything is kept), while the UI says "YOUR PRIVACY. YOUR CHOICE." / "Purpose-built for privacy"; /canary invites visitors to "check exports" although the signed audit export is every account's activity timeline (PRIV9-1 = BUY9-4 = FE9-4, + SEC9-3/PRIV9-5 export framing, + BUY missed: handle is permanent) | transparency | code/template read + grep (4 seats) | QA-verified on 483d944, CI rerun pending — not yet closed (opsec-qa-release: records list checked class by class against schema + migrations 001-060, visibility claims traced to code, 320 px, links, export framing; wording NITs → A-113; CI rerun pending; fix/a81-records-disclosure b0267b2) |
| A-82 | Displayed times use the database session time zone with no label (`to_char(created,'YYYY-MM-DD HH24:MI')` across orders, messages, notifications, audit, payouts); on an external/host PostgreSQL (supported installer path) any registered user learns the operator's UTC offset from a draft's "Created" time; pages mix "UTC"-labelled and unlabelled times (FE9-1; frontend measured: official `postgres:17-alpine` default = UTC, host initdb = America/New_York shown as 08:01 for 12:01Z) | privacy/ui | browser DB (frontend), code read | verified (PR #9 @d296560, CI 36001767193; opsec-qa-release: hostile zones in server/db/role/DSN/PGTZ/PGOPTIONS, 65 live times UTC-labelled, audit export byte-identical ab7cd9f vs d296560; label pins → A-111; fix/a82-utc-timestamps 15254bb) |
| A-83 | A buyer can pay for a physical order from a vendor with no key usable for encryption, then has no way to send the address (only exits: vendor cancel or dispute → plaintext address); the pay form shows no warning (BUY9-5; security/frontend/privacy MODIFY: use the A-68 "can receive encrypted messages" test, hide the pay form, keep a server backstop, and give the order page a "no usable key" state) | commerce/privacy | Go DB probe (buyer; pages only) | open (ruled iteration 9) |
| A-98 | A credited deposit that falls back below the threshold (QA saw orphaned txs stay at depth 0 on a real chain, never −1) is silent: nobody is told, the vendor can still ship, the payout is held, admin release 409s "Review it with a moderator" but no review item exists and moderators get 404; a deposit flagged once is never re-announced (`AND NOT flagged`); the payment-review list is newest-first capped at 100, so old unhandled flags drop off; watcher-held rows show a Release form that always 409s (QA9-1, STAFF9-6, STAFF9-2, STAFF9-1 truncation part, QA MISSED tip regression) | payments/staff | Go DB probes (QA, staff) + real local chain (QA) | in-progress (fix/a98-regression-announce) |
| A-99 | Bitcoin payouts below the fee can never be sent (`sendtoaddress` subtracts the fee: regtest 3,000 sat → −6, 5,000 sat sent); a 1-sat listing price is accepted and dust to a cancelled order's address is refunded automatically, so the row fails "definitely" on every requeue and sits in the attention count forever — the only exits are a fabricated Mark-sent txid or SQL (DA9-1; payments: XMR pays fee on top, so only an economic floor for automatic refunds) | payments | real local chain (RPC) + code read | open (ruled iteration 9) |
| A-100 | A-23's "nothing broadcast" Monero code table is verified against monero-wallet-rpc v0.18.5.1, but `compose.nodes.yaml` runs `bitcoin/bitcoin:latest`, `simple-monerod:latest` and `simple-monero-wallet-rpc:latest`; a silent upgrade can make a post-broadcast error look definite (DA9-3) | payments/ops | config + doc read | open (ruled iteration 9) |
| A-101 | The last double-pay barrier is a human wallet check whose documented commands can miss the send: `listtransactions '*' 50` missed a payout behind 26 later wallet txs on real regtest; Monero `get_transfers` builds "pending" before its pool update and a relayed-then-errored send has no destinations; restores not done with `restore.sh` (host/volume snapshot, managed-DB point-in-time recovery, manual `pg_restore`) skip the payout gate entirely and resend pending payouts; setting and clearing the gate leave no audit row (DA9-4 + PAY9-2 folded, DA9-2, STAFF9-4) | payments/ops | real local chain (BTC) + upstream source (Monero) + doc read | in-progress (fix/a101-wallet-checks-restores; also covers A-112 restore part) |
| A-102 | Suspending an account (A-27) does not stop its payouts: an attacker with a vendor's password (+TOTP) changes the payout address (`savePayoutAddress` re-points blocked payouts at once, no notification) and pending/future releases go to it after the admin suspends the account; the send claim never checks the recipient (payments MISSED) | payments/identity | code read | QA-verified on 483d944, CI rerun pending — not yet closed (opsec-qa-release: acceptance + chair-accepted deviations, mutations M7-M17 killed, A-27 FOR SHARE still serialises; converted-hold defect → A-114; CI rerun pending; fix/a102-suspend-holds-payouts 512cc5e) |
| A-110 | A GnuPG photo ID (user-attribute packet, usually the owner's face) in a saved key is republished to every signed-in account, trading partner and anonymous vendor-page visitor, but /account lists only user IDs and the A-79 copy says "with its user IDs and creation date" — the disclosure that closes A-79 misses it (QA9W-2, live with a real `gpg addphoto` key). ACCEPTANCE: /account lists each user attribute as "Photo ID (JPEG, N bytes)" and the visibility copy and implementation-status name photo IDs — or keys with user-attribute packets are refused with a `gpg --edit-key … deluid` hint; accepted keys keep packets unchanged. TEST: pgp_integration_test.go with a go-crypto entity carrying a UserAttribute | identity/privacy | live server + real gpg (QA) | open (ruled iteration 9) |
| A-114 | A watcher hold converted by a suspension (A-102) or by `restore.sh` on an order >24 h / address >30 days old leaves the watch set (A-97 watches only `pending` and `heldReason` holds): its ledger never refreshes and every release 409s forever (QA10V-1, DB probes). ACCEPTANCE: watch every order with a `pending` or `held` payout; `settleOrder` still lifts only `heldReason`; QA's suspension and restore probes release 303 after re-confirm, nothing sent before release. TEST: `TestConvertedWatcherHoldReleasableAfterReconfirm` | payments | DB integration (QA) | in-progress (folded into fix/a98-regression-announce, same file) |
| A-115 | Every real order page overflows sideways on a phone: 64-hex order ID in the page head/breadcrumb (~555 px at 320) and `.grid-two` `1fr` tracks growing to the deposits table's min-content (900–1200 px once a deposit exists, 71 px at 1280); long handles and monospace h1s overflow at 200% text; preview hides it with short IDs and no deposits (BUY10-1 revised to REQUIRED + FE10-3) | ui | browser DB, 3 engines | fixed (war-room/integration merge of fix/a115-order-overflow 29b2633; implementer gate: Playwright 426 incl. width checks at 320/390/1280 on real orders + wallet 4/4; failing-first 46 preview failures) — CI + QA pending |
| A-116 | Admin payout resolve forms sit in `table{white-space:nowrap}`: at 1280 px only 16% of the "wallet shows no transaction" confirmation text is visible when its checkbox has focus — the last human barrier against a double pay (FE10-1) | ui/payments | browser DB (Firefox + Chromium) | open (ruled iteration 10) |
| A-117 | "Changes saved." is a URL flag (`?saved=1`): Back after a failed POST shows success (measured in Firefox and Chromium), fragment redirects put it off-screen, one text for every action, sign-out shows nothing, the title never changes (FE10-2, FE10-4 title part) | ui | browser DB | open (ruled iteration 10) |
| A-118 | Suspended moderators/admins still count as independent resolvers, dispute contacts and flag recipients (4 queries lack `suspended_at IS NULL`): the A-8 "no eligible resolver" guarantee silently fails and buyers are told to encrypt evidence to a suspended account; the admin suspend copy also claims counterparties "can continue open orders" (PM10-2 = STAFF10-1, both reproduced; PM10-4) | commerce/identity | Go DB probes ×2 | open (ruled iteration 10) |
| A-119 | "Contact the market staff" has no path: shown in the suspended sign-in refusal and payout-address notice, but staff handles appear only to dispute parties; /challenge says nothing about lost factors; blind users cannot pass the image CAPTCHA and are not told what to do (PM10-1, VEN10-3 contact part, FE10-5 copy part) | ui/identity | code read | open (ruled iteration 10) |
| A-120 | Recipients cannot see their money: no view lists a user's unsent payouts (blocked/held/failed), `/account` counts only `blocked`, the order page says "This order is closed. No further changes are possible." over a blocked refund or payout, labels are wrong ("deposit conflicted" for suspension/restore holds), no reason or "administrators notified" for failures, the destination is never shown to the recipient (PM10-3, VEN10-2, BUY10-4, VEN10-3 copy part, A-107/A-112 label parts) | payments/ui | browser DB + simulated wallet (vendor, buyer), Go DB (PM) | open (ruled iteration 10) |
| A-121 | A held or definite-failed payout keeps its old destination and nothing can change it except SQL/wallet shell; after a suspension where the owner saves a new address, the payout still goes to the old (possibly attacker's) one (VEN10-1 revised) | payments | browser DB + simulated wallet | open (ruled iteration 10) |
| A-122 | Money-moving single taps: completing an order releases the whole received amount (overpayment included — a 10× slip was released to the vendor) and a vendor's cancel of a paid order refunds, each with one button and no amount stated (BUY10-2, FE10-7) | commerce/payments | browser DB + simulated wallet | open (ruled iteration 10) |
| A-123 | The payment deadline (`PAYMENT_EXPIRY`, as short as 10 min, re-read every pass) is never shown; system and moderator transitions notify only "Order x is now Cancelled/Resolved" with no reason or outcome (BUY10-3, BUY10-6) | payments/ui | browser DB | open (ruled iteration 10) |
| A-124 | A dispute has a one-party record: the party who did not open it cannot put anything on record, and with no staff PGP key cannot send evidence at all; the moderator decides on the opener's text alone (VEN10-4; buyer: the vendor can dispute too) | commerce | browser DB | open (ruled iteration 10) |
| A-125 | Public reviews cannot be moderated: the only write is an INSERT, so a review that doxxes a vendor stays public until someone edits the database (PM MISSED) | commerce/staff | code read | open (ruled iteration 10) |
| A-126 | Sign-in drops the page the user wanted: an anonymous buyer's first "Review order draft" or "Contact vendor" click ends on `/` after sign-in (register and TOTP challenge too) (PM10-7 = FE10-6; buyer raised to REQUIRED) | ui | browser DB (Firefox) | open (ruled iteration 10) |
| A-127 | Documented "admin order list" does not exist: `/admin` runs `orderQuery WHERE true LIMIT 100` and renders nothing; implementation-status and A-16 claim it (PM10-6; staff NIT) | ui/docs | Go DB + code read | open (ruled iteration 10) |
| A-128 | Required CI check fails at random: `db-seeded-marketplace.spec.ts:82-84` click → `url()` → `reload()` race turned PR #9 CI red twice (runs 35998094632, 36006118748) (QA10V-3; A-12 part raised) | tests | CI logs | open (ruled iteration 10) |
| A-103 | Release evidence: a SHA-pinned real-chain rerun is required after the payout changes since cdb69f2 (A-53, A-54, A-97, A-98, A-99), with a manifest (`git rev-parse`, binary SHA-256s, payouts/payments dump, Playwright JSON) under `artifacts/real-chain/<sha>/` cited in `docs/verification.md`; QA's ab7cd9f run passed (chain-journey 1/1, 4 real payouts) but its logs are in session scratch storage, carry no commit SHA and its payout summary came from a deleted database — not citable (A-13 ruling; QA9 B-round; DA9-8) | tests/ops | local real-chain run (not retained) | open (final QA pass of this loop) |

### Iteration 3 rulings (chair) — acceptance criteria

- **A-6 (ACCEPT)**: every open dispute visible to the viewer is always listed, open first, then recent resolved up to a cap with a truncation notice; vendor NeedsAction equals the true count of the vendor's `paid` orders and those orders are never hidden by the 100-row cap; no authorization change. Test: DB integration with one old open dispute + >100 newer resolved; vendor with >100 newer orders + an old paid order.
- **A-7 (MODIFY)**: keep one payout per order and today's payout sum. Watch every funded non-terminal order regardless of `updated` age; a deposit first recorded after funding writes one order event and notifies buyer and vendor (idempotent); funding-deposit conflict on an order older than 24 h is detected; implementation-status updated. Dissent recorded: re-routing post-funding extras to the buyer was rejected because it needs a second payout per order (breaks invariant 4). Test: DB integration via newPayEnv, orders with `updated` 48 h ago.
- **A-21 (ACCEPT)**: releasing a payout held by a restore (`error` prefixed `Restored from backup:`) requires `not_broadcast=confirmed`, like the ambiguous requeue; note and audit record the confirmation; watcher-held release unchanged; admin UI shows the checkbox only for restored-held payouts; docs updated. Test: DB integration mirroring the ambiguous-requeue tests.

**Round D (iteration 3 council). No majority vote; dissent kept.**

- **A-23 (MODIFY → P0)**. WHY: DA traced monero-wallet-rpc v0.18.5.1 (`wallet2::commit_tx` → `sendrawtransaction` timeout → `no_connection_to_daemon` → -38; post-submit checks → -4); chair confirmed `walletRejected` treats any `*rpcError` as definite. DISSENT: QA (Bitcoin Core commits the tx before relay, so a BTC JSON-RPC error means nothing was committed; an allowlist would train reflexive box-ticking) — accepted for BTC, so the change is Monero-only; DA wanted BTC allowlisted too. ACCEPTANCE: a Monero `transfer` error is definite ("nothing broadcast") only for codes raised before submission, verified against the v0.18.5.1 source and listed in one Go table with a comment citing it (expected: -2 wrong address, -16 tx not possible, -17 not enough money, -18 tx too large, -19 not enough outs, -20 zero destination, -37 not enough unlocked money — confirm each); every other code (incl. -38, -4, -1) stores `send_ambiguous=true` and requeue then needs `not_broadcast=confirmed`; BTC classification unchanged but documented with its reason; admin copy says "the wallet reported a pre-broadcast error". TEST: httptest JSON-RPC table cases in `payments_payout_timeout_test.go` for XMR allowlisted vs -38/-4/-1 and BTC unchanged; extend `TestAmbiguousFailedPayoutRequeueNeedsBroadcastConfirmation` with XMR -38. EXPERIMENT (A-45) settles the live behaviour.
- **A-24 (ACCEPT, minimal shape; disposition action deferred to A-31)**. USER IMPACT: moderators/admins can finally see what they were told to review. ACCEPTANCE: a non-party moderator or admin gets 200 read-only on `/order` for any order with a flagged payment (`payments.flagged`), with order history, deposit txids and flag notes visible, no buyer/vendor forms, and no digital delivery content unless the order is disputed; an unflagged, undisputed order still 404s for staff; /moderator gets a "Payment review" list driven by `payments.flagged` (order link, full order ID, currency, amount, full txid, flag time), newest first, capped with a truncation notice; runbook section on handling flags, including recording a manual refund as a written note (no payout row: `payouts.order_id` is unique — DA MISSED). TEST: Go DB `TestFlaggedOrderReadableByNotifiedStaff` (newPayEnv, conflict after paid; moderator+admin 200, party moderator unchanged, unflagged 404), desk listing assertion; update `gaps_integration_test.go` 404 pins only for flagged orders.
- **A-25 (ACCEPT)**. ACCEPTANCE: every payout needing attention (blocked, held, failed, stuck sending) is always listed with its resolve form, oldest first, then recent settled payouts up to 50 with a truncation notice; "N payouts need attention" shown; order IDs in the payout table link to the order (admin read access follows A-24 rules — link only where the admin can open it). TEST: Go DB `TestAdminPayoutsAttentionNeverHidden` (one failed + one stuck, then 60 newer sent).
- **A-26 (ACCEPT; merges DA-3 and MOD-5 role part; iteration 5: also A-71)**. ACCEPTANCE: the second-factor reset form also takes a handle looked up server-side (no 1000-row picker);  the role form takes a handle (text input, handle pattern) looked up server-side; with >100 newer users the oldest vendor can be demoted; unknown handle → 404 with the form re-rendered and input kept; admin role assignment still refused; A-4 audit rows unchanged. TEST: Go DB (vendor + 101 buyers); `db-admin.spec.ts` updated with fresh accounts.
- **A-27 (MODIFY)**: one admin action "Suspend / restore account". DISSENT: DA (suspension state is over-engineered; demotion + end-sessions suffice) — rejected because ending sessions without blocking sign-in does not contain a known password, and the check points are only sign-in and session load (sessions and pending logins are deleted). ACCEPTANCE: password(+TOTP)-confirmed, admin-only, refused for administrators; suspend ends all sessions and pending sign-ins and blocks new sign-in with a specific message; restore re-enables sign-in; both accounts audited naming the other; user notified; open orders continue for the counterparty; listings unchanged (demotion already archives). New numbered migration. TEST: Go DB `TestAdminSuspendsAccount`; `db-admin.spec.ts` with fresh accounts.
- **A-28 (MODIFY — host-only tool, no web path)**. ACCEPTANCE: `opsecmkt -reset-admin-password <handle>` (or equivalent flag on the server binary) reads the new password from stdin, applies the registration rule, hashes at the app's bcrypt cost, ends that admin's sessions and pending sign-ins and writes an audit row; refuses non-admin handles; operator-guide gives the exact `docker compose run` command and no hand-written SQL. TEST: Go DB test of the CLI path; `bash -n`; ops doc validated.
- **A-29 (MODIFY — runbook + tested SQL, no web action)**. DISSENT: PM originally proposed a web "record external payout"; withdrawn under MOD's hijack argument. ACCEPTANCE: `testnet-runbook.md` reconcile section becomes one ordered procedure (isolate the instance, reconcile, stop, clear gate, restart) with no contradiction; it provides the SQL to record a payout sent after the backup (sent payout row + order event + audit, inside one transaction, with a check query) and the order-state reconstruction; `scripts/test-backup-restore.sh` gains a case where a restored order with no payout row is reconstructed with that SQL and completing it queues no second payout. TEST: that script case in CI's operations job.
- **A-30 (ACCEPT; required before the next tag)**. ACCEPTANCE: startup refuses (non-zero exit, error names the version, nothing migrated or served) when `schema_migrations` holds a version the binary does not embed; normal start and the alpha.1 upgrade unchanged; `UPGRADING.md` rollback text matches. TEST: Go DB `TestStartupRefusesUnknownAppliedMigration`; `scripts/test-upgrade.sh` rollback rehearsal step.
- **A-52 (P0; raised from the verifier's REQUIRED by the chair: a payout including a deposit the ledger already marks conflicted breaks the hold invariant).** ACCEPTANCE (verifier's safe shape): `enqueuePayout` returns early when a payout row exists; otherwise `SELECT id,amount FROM payments WHERE <settled> FOR UPDATE`, sums in Go, inserts the payout, then `UPDATE payments SET credited=true WHERE id=ANY($ids)` — the payout amount equals exactly the credited rows; the hold check sees every credited row. NOTE: the implementer flagged the same SUM-then-UPDATE shape in `settleOrder`'s awaiting_payment→paid step; the chair ruled it unreachable — only the single advisory-locked watcher writes `payments`, and it records all deposits before settling any order in a pass, so nothing interleaves. TEST: the hunt's `TestHuntRefundSilentlyDropsDepositConfirmedDuringEnqueue` and the verifier's `TestVerifyE2NaturalRace`/`TestVerifyE2ReverseRace` (artifacts/bug-hunt/699ea45/verify/verify2_payments_test.go.txt) adapted into `payments_watcher_gaps_test.go`, passing repeatedly under `-race`.
- **A-53 (ACCEPT; extends the A-21 ruling)**: restore SQL (restore.sh, UPGRADING.md) also marks `failed` rows ambiguous and prefixes `Restored from backup:` (guarded for dumps without the column); restore output counts them; the admin label no longer says "nothing broadcast" for them; `test-backup-restore.sh` and `test-internal-db-restore.sh` expectations updated. TEST: hunt repro `TestHuntRestoredDefiniteFailureRequeuesWithoutWalletCheck` passing.
- **A-54 (ACCEPT, code fix)**: with a configured-but-unavailable provider, `enqueuePayout` uses its configured threshold for counting and holding; disabled providers keep "credited only"; notice copy says "as last recorded by the market". TEST: hunt repro `TestHuntAnnouncedDepositDroppedWhenCompletedWhileWalletUnavailable` passing.
- **A-47 (MODIFY, bug hunt #1)**. DISSENT: the verifier would also require a PGP proof before enrolling TOTP on a PGP-only account; rejected for now because the existing rule already accepts the password alone to turn PGP sign-in *off* on such an account, so password-only enabling is no weaker (revisit with A-32). ACCEPTANCE: when the account has any second factor enrolled, enabling another (`/pgp/2fa` enable, `/totp/activate`) and changing the saved PGP key require `confirmedTx(c, true, …)` — current password plus a current TOTP code when TOTP is enrolled (recovery codes are not accepted here, as in other `confirmedTx(c,true)` flows; a user without an authenticator turns TOTP off with a recovery code first — implementer's reading adopted); forms render `confirm-fields`; an account with no factor keeps today's flow; audit rows say how it was confirmed. TEST: the hunt's `TestHuntStolenSessionAddsPGPFactorBypassingTOTP` and `TestHuntStolenSessionEnrollsTOTPBypassingPGP` moved into `auth_gaps_test.go` and passing; existing TOTP/PGP Playwright specs updated.
- **A-48 (ACCEPT, P1 — staying signed in is a headline workflow on a market reached through links)**. Verifier ruling adopted: keep `session` SameSite=Strict (Lax would let any site probe sign-in state and fire side-effecting GETs such as the one-time recovery-code reveal). ACCEPTANCE: pre-login state (CSRF/CAPTCHA token) lives in a separately named Strict cookie; a cookieless request never sets or overwrites `session`; `session` is set only at sign-in (token still rotated); a form rendered on a cookieless landing page still submits (accept the anonymous token for CSRF, or re-render once); after a cross-site link click, a direct reload of /account is still signed in; login, register, CAPTCHA and CSRF behave as before. TEST: the hunt's `TestHuntCrossSiteLinkClobbersSessionCookie` passing; `TestLoginRotatesSessionToken` unchanged; a db-*.spec.ts cross-site navigation case (routed foreign origin) in Chromium.
- **A-13 (AMENDED by DA-2, QA3-3, PM-5)**: add an evidence table to `docs/verification.md` (check, tier, commit SHA, CI run or log path, date); record QA's 509356d real-chain run; tick `acceptance.md` lines only against a row (CI-backed lines: the green CI run at that SHA; manual tiers: their own row); remove the contradictory "not run"/"passed" sentences; the original A-13 drift items stay. A chain-journey rerun is required after watcher/adapter changes (A-7, A-23) before any release (QA MISSED). **Done for A-7**: chain-journey (bitcoind 31.1 regtest + monerod 0.18.5.1 offline stagenet) 1/1 and regtest-smoke PASS on 699ea45 (QA-reported; the SHA-pinned logs were not retained — DA9-8, verification.md); **Done after A-23/A-52**: chain-journey 1/1 (4 real payouts, none ambiguous) and regtest-smoke PASS on cdb69f2 (QA-reported, binaries' SHA-256 re-checked; logs not retained — DA9-8). Superseded for release by A-103.
- **A-12 → P2** (DA-9): CI already sets `TEST_DATABASE_URL` and an unreachable URL fails; keep the shared-admin sign-in budget, the TOTP boundary and QA3-7's click-then-reload races.
- **A-14, A-19 → Declined** (DA-8, DA-10). **PM-5** (publish a prerelease tag) → owner action after P0/P1 close (A-46, blocked on owner; listed under P3 for tracking).

### Iteration 5 rulings (chair, 2026-09-24) — from bug hunt #2 verified findings

- **A-66 (ACCEPT, P0)**. WHY: after either documented recovery (side-by-side restore, UPGRADING rollback) every routine backup silently omits live data; verified on a real internal-db Compose stack. ACCEPTANCE: `scripts/backup.sh` (internal-db path) dumps the database named in `DATABASE_URL` from `.env` (path component; never printed with credentials), overridable by `BACKUP_INTERNAL_DATABASE`; if both are set and differ it refuses with a clear message; the success line names the database dumped; `scripts/restore.sh` prints which database it restored into and the exact `.env` change plus that backups now follow `DATABASE_URL`; operator-guide and UPGRADING say so, and warn external-db operators to point `BACKUP_DATABASE_URL` at the database they switched to. TEST: new case in `scripts/test-internal-db-restore.sh` — restore side by side, switch `DATABASE_URL`, write a row, `backup.sh`, restore that backup into a check database, assert the row is present; plus the refusal case; `bash -n`; the script is in CI's operations job.
- **A-67 (ACCEPT, P1)**. ACCEPTANCE: `/messages` accepts exactly one armored PGP MESSAGE block and nothing but whitespace outside it; decodes the whole base64 body; accepts only key packets (PKESK/SKESK) followed by exactly one encrypted-data packet (SEIPD or AEAD), read to EOF, with no trailing packets; stores a canonical re-armor of the decoded bytes (armor headers dropped), so no plaintext from headers, a second block or text around the block is stored or shown; real gpg/go-crypto output (Version/Comment headers, CRLF, multiple and hidden recipients) is still accepted with the same encryption-status badges. TEST: the hunt repro `TestHuntMessagePlaintextInsideArmorBadgedEncrypted` (three variants) plus the two sandwich variants (block+text+block, block+text+bare END) → 400, nothing stored; acceptance cases with CRLF, Comment header and two recipients → 303 and the stored body contains no header text; existing message/PGP tests and `db-pgp-messages.spec.ts` pass.
- Chair note (A-67): the Comment-header variant is accepted with the header dropped (303, nothing stored), per the acceptance text; the test line's "→ 400" for it was inconsistent. Pre-fix stored rows are re-checked on display and lose the Encrypted badge if they fail.
- **A-72 (ACCEPT code fix, P1)**. DISSENT: the verifier noted a docs-only fix (admins may read/replace auto-delivery content) is defensible; rejected because the A-24 ruling made delivery secrecy from non-party staff deliberate, and admin substitution under the vendor's name is the stronger harm. ACCEPTANCE: `/listing-edit` never loads or renders `delivery_content` for an editor who is not the listing's vendor (shows "Automatic delivery content is set; only the vendor can view or change it", or "not set"); `listingUpdateAction` keeps the stored content when the editor is not the vendor, whatever the form sends; a non-vendor admin changing the kind to physical while content is stored is refused as for the vendor; the vendor's editor is unchanged; docs (implementation-status P4 "Administrators may edit any listing", operator-guide) state the exception. TEST: the verifier's `artifacts/bug-hunt/cdb69f2/verify/batch-c/verify_b2c_test.go` adapted into `inventory_integration_test.go` (admin page hides the secret; admin title save keeps it; admin POST with different content does not change what the next order receives; vendor still sees and edits); a `db-inventory.spec.ts` admin assertion if cheap (fresh accounts).

### Iteration 6 rulings (chair, 2026-09-24) — original-audit P1s given acceptance criteria

- **A-8 (ACCEPT)**. ACCEPTANCE: opening a dispute notifies every moderator and administrator who is not a party to the order (link-free text naming the order, as other notifications); if no eligible resolver exists (every staff account is a party), the dispute is still accepted — the buyer's protection is never refused — and every administrator is notified that the dispute needs a moderator who is not a party, and the order history records that no independent resolver was available; the moderator desk shows such disputes with an "no eligible resolver" note to admins. TEST: Go DB — dispute on an order with a separate moderator notifies the moderator and admin but not the parties; order whose vendor is the only admin and with no moderator: dispute 303, admin notified with the no-resolver text, history note written.
- **A-9 (ACCEPT)**. ACCEPTANCE: startup no longer shares one 15 s context across ping, advisory lock, migrations and payment init: the database ping and lock acquisition keep bounded timeouts (configurable, default ≥ 15 s), migrations run under their own longer bound (default several minutes, configurable, documented) so a slow migration on a large database does not crash-loop; payment init keeps its own bound; timeouts produce an error naming the phase. TEST: Go unit/DB tests with a tiny migration bound showing the phase-named error, and a normal start unchanged; docs (operator-guide startup/upgrade) name the setting.
- **A-10 (ACCEPT)**. ACCEPTANCE: sealed plaintext recovery codes (`users.recovery_reveal`) never outlive their 10-minute window: expired reveals are cleared by a periodic background sweep (registered with `registerBackground`, cancellable on shutdown; at startup and every minute) and when the account next opens a security page (/account, /pgp, /admin, /totp) — amended after implementation: sign-in-time clearing was not built and adds no exposure given the one-minute sweep; docs (implementation-status recovery codes) state exactly how long sealed codes can exist; nothing changes for a reveal still within its window. TEST: Go DB — a reveal older than its window is cleared by one sweep; an in-window reveal survives; shutdown stops the sweep.
- **A-11 (ACCEPT)**. ACCEPTANCE: a limiter entry for sign-in is created or incremented only after the CAPTCHA passes (when CAPTCHA is on) and only for a well-formed handle; when the in-memory table is full, eviction never removes an entry that is currently blocking (count ≥ its limit) — prefer the lowest-count non-blocking entry; if every entry is blocking, new keys are refused with 429 rather than evicting; docs (implementation-status sign-in challenge / limiter text) match. TEST: Go unit tests on `allow` (full table of blocking entries → new key refused, blocking entry never evicted; junk keys cannot reset a victim's counter) and a DB test that a wrong CAPTCHA does not consume a handle's sign-in budget.

### Iteration 9 rulings (chair, 2026-09-24) — privacy sweep (privacy, security, frontend, buyer; Rounds A–C)

No blocker: every seat confirmed the narrow promises hold on real responses (no IP/user-agent capture or access log; CSP `default-src 'none'`; 348 requests from a Safest-like Firefox through signed-in DB flows, all same-origin; no secrets in logs; headers on every response class). The failures are what the product republishes or invites users to store, and what it never discloses.

- **A-79 (ACCEPT, P1; merges PRIV9-2, SEC9-1, FE9-2, BUY9-2, FE9-5)**. SEC9-1's confirm checkbox for "@" user IDs REJECTED — withdrawn by security itself: it fires on nearly every GnuPG key (email is the default), cannot be judged reliably, and without JavaScript costs a refused round-trip that loses the pasted key in Firefox (A-59). Closing the handle lookup rejected (first contact needs it). ACCEPTANCE: `saveProfileKey` stores a canonical re-armor of the parsed key (no text outside the block, no armor headers; key packets unchanged; fingerprint and existing ownership verification survive); legacy rows are shown canonically at display time on every page (A-74 pattern), no rewrite migration; /account lists the saved key's user IDs next to the fingerprint and, above the key field, says who can see it ("Shown to every signed-in account that looks up your handle, to every vendor or buyer you trade with, and publicly on your vendor page if you sell — with its user IDs and creation date. Use a key made only for this market."); other users see "Ownership verified YYYY-MM-DD" (date only), the owner's /account and /pgp keep the minute; implementation-status (key display, line ~42) states that `/messages?to=` shows the key to any signed-in account. Note (security caveat): re-armor does not remove user IDs, creation time or third-party signatures — the disclosure is what closes it. TEST: Go DB `TestProfileKeyStoredCanonically` in `pgp_integration_test.go` (key with leading text + Comment/Version headers → stored value and anonymous /vendor and a second account's /messages?to= contain neither; fingerprint unchanged; /account lists the user ID; others see a date without HH:MM); legacy-row display case; `db-pgp-messages.spec.ts` (fresh account) asserts the user-ID list.
- **A-80 (ACCEPT, P1 copy-only; merges PRIV9-3, SEC9-2, FE9-3, BUY9-1, BUY9-3, PRIV9-11)**. ACCEPTANCE: help text on the vendor's shipping note (and rename it "Note to the buyer"), the cancel reason, both dispute-reason forms (`order.html` and `disputes.html`) and the review body stating that the text is stored unencrypted, who can read it (the other party; staff when disputed or flagged; the operator and backups) and never to include an address, real name or tracking number — send those encrypted from the order page (dispute forms point to the encrypted "Contact dispute staff" keys); review help adds "The vendor sees your review, with your handle, on this order."; `implementation-status.md:57,59` and `operator-guide.md:104` qualified accordingly ("no address field; the market cannot stop addresses typed into plaintext fields; the encrypted address message is kept"). TEST: `preview-orders.spec.ts` assertions on the ship/cancel/dispute/review help; `TestReviewsRequireACompletedOrder` pins that the vendor's /order shows the review body.
- **A-81 (ACCEPT, P1; merges PRIV9-1, BUY9-4, FE9-4, SEC9-3 + PRIV9-5 export framing, BUY MISSED)**. SEC9-3 revised to accept a docs/copy fix (no export code change, invariant 8). ACCEPTANCE: `/canary` gains `<section id="records">` (h2 + list per data class, no table — 320 px) naming each stored class a user creates (handle, password hash, sealed TOTP, PGP key, messages as ciphertext plus sender/recipient/time, notifications, orders and drafts incl. cancelled, order notes, disputes, reviews, deliveries, payment addresses and txids, payout addresses, audit rows incl. sign-ins), who can read it (parties, staff on disputed/flagged orders, administrators, the operator and backups) and "kept until the operator removes it; there is no account or message deletion"; states that the application records no IP address or browser details (a reverse proxy may); says the signed audit export names every account's activity and is private operator evidence, never to be published, and /canary no longer implies visitors receive exports (admin export panel and operator-guide say the same); linked from /register, /account and the /messages notice; "YOUR PRIVACY. YOUR CHOICE." replaced by one factual line linking `/canary#records` (rendered once per viewport; screen readers not told twice); shipping line on order/vendor pages reads that the market keeps the encrypted message and who sent it to whom and when; /register and /setup say the handle cannot be changed, is shown to trading partners and staff, and should not be reused from elsewhere; implementation-status carries the same list, checked against the schema. TEST: preview spec on `/canary#records` heading and the three links; `routes_test.go` asserts no template contains "YOUR CHOICE".
- **A-82 (ACCEPT, P1; FE9-1, supported by security and privacy)**. ACCEPTANCE: every application database connection runs with `TimeZone=UTC` (pgx runtime parameter on the pool, not a database-level setting the operator may not own); every displayed timestamp (orders, history, messages, notifications, audit, disputes, payouts, provider polls, listings) is labelled " UTC"; with the database's `TimeZone` set to e.g. `Pacific/Chatham`, rendered times equal `now() AT TIME ZONE 'UTC'`. TEST: Go DB via `newTestApp` with `ALTER DATABASE … SET timezone='Pacific/Chatham'` asserting /order and /messages times; `db-orders.spec.ts` asserts the " UTC" suffix.
- **A-83 (MODIFY, P1; BUY9-5)**. DISSENT: security wanted a vendor-side publish gate (a physical listing cannot be published or restored without a usable encryption key) — DEFERRED to A-91 because it changes vendor onboarding and existing listings; the buyer-side check covers the harm now. Warning-plus-confirmation rejected (the key can change between draft and pay; trains click-through). ACCEPTANCE: A-68's check ("vendor has a key that can receive encrypted messages": not expired, has an encryption subkey) is implemented once and used for both: A-68's "No usable key" contact note, and physical orders — the order page replaces the pay form with "Unavailable: this vendor has no usable encryption key, so you could not send your shipping address. Ask them to add one." (existing "Unavailable. <reason>" pattern), product and checkout pages show the note, `/orders/pay` refuses with 409 as a backstop before any reservation or `NewAddress` call; digital orders unaffected; a paid physical order whose vendor key stopped being usable shows a "no usable key — message the vendor or open a dispute" state instead of the send-address prompt. Closes A-68. TEST: Go DB via `newPayEnv` — keyless and expired-key vendor, physical → 409, fake provider `NewAddress` not called, nothing reserved; digital → 303; A-68's `TestHuntUnusableEncryptionKeyShownAsUsableContact` passing.

### Iteration 9 rulings (chair, 2026-09-24) — payments sweep (payments, qa-release, staff, devils-advocate; Rounds A–C)

No second send for any order was found (QA: `go test -race` 259/0, wallet journey 4/4, chain-journey 1/1 on real local chains with 4 real payouts; DA: 21 mutations, the watcher's mechanics mostly pinned). The conflict-hold invariant, however, is bypassable, and the human wallet check that ends every anti-double-pay path is documented with commands that can miss the send.

- **A-97 (P0; PAY9-1 + DA9 MISSED + A-64 revised + DA9-7)**. DISSENT: devils-advocate rates it P1 (the trigger needs a post-threshold reversal >24 h after close on a payout still unsent; the loss is the pool's, not a second payment) — overruled on the invariant and the A-52 precedent (a payout that includes a deposit the ledger would mark conflicted breaks the hold invariant), with QA's real-chain observation that reorged deposits sit at depth 0 and the common blocked-refund path; payments and QA support P0. Fix shape: DA's narrower watch set adopted over "watch every unsent payout forever" (unbounded polling of blocked/failed rows that cannot send until a person acts). ACCEPTANCE: (1) the watch set also includes every order with a payout in `pending`, or `held` by the watcher (`error` = the watcher's hold reason), with no age limit; (2) `sendPayouts` claims a payout only for an order whose addresses were read **and** whose `settleOrder` succeeded in this pass (e.g. `AND order_id = ANY($settledOK)`); a payout that becomes pending mid-pass (address saved, admin release/requeue) waits for the next pass; fresh completions are still sent on the next pass; (3) a watcher-held payout whose deposit re-confirms is un-held regardless of address age; (4) the claim re-check at :510 and the `recordPayout` state CAS (:586) are pinned by tests. TEST (Go DB, `payments_watcher_gaps_test.go`, newPayEnv + fake): QA's `qa9_bc_probe_test.go.txt` and the payments probe as named regressions — completed order `updated` 48 h ago, blocked release, wallet drops the deposit, address saved → flagged, held, 0 sends; `TestPendingPayoutNotSentWhenAddressUnread`; `TestSendSkipsOrderWhoseSettleFailed`; `TestOldHeldPayoutResumesAfterReconfirm` (address 40 days old); a claim-recheck case that fails if :510 is removed; `TestLateRecordDoesNotOverwriteAdminResolution` (fails if :586's `state='sending'` is removed); existing lifecycle and A-52 race tests pass under `-race`.
- **A-98 (ACCEPT, P1; QA9-1 + STAFF9-6 + STAFF9-2 + A-31's truncation part + QA MISSED)**. DISSENT: staff wanted A-31 (web disposition) raised to P1 before any new flag source — MODIFIED: its load-bearing part (undisposed flags never hidden, reason from current state) moves here; the web "refunded manually (txid)" disposition stays P2 (A-31) because the refund itself needs the wallet shell and a hijacked moderator session recording a refund is the D-6 objection (DA). ACCEPTANCE: (1) a credited deposit that falls below the threshold (confirmations ≥ 0) while its order is non-terminal or its payout is unsent is announced once per episode — one order event, notifications to buyer, vendor and every non-party moderator/admin — and makes the order staff-readable and listed for payment review (via `payments.flagged` or an equivalent marker the loaders use); re-confirming ends the episode and a later regression or conflict is announced again (no `AND NOT flagged` suppression across episodes); a completed order whose payout was already sent is not flagged; (2) no announcement while the node's tip is below the highest tip the watcher has recorded for that currency (store it in `payment_status`; a lower tip counts as syncing and the pass is skipped) — so a node crash that rolls the tip back raises no false alarms; (3) /moderator's payment-review list shows every open (unhandled) flag first, oldest first and uncapped, then recent others with a count, and each row's reason comes from the deposit's current confirmations ("conflicted", "below threshold (N)", "confirmed again"); (4) /admin shows no Release form for a watcher-held payout whose deposit is still below threshold — it names the deposit, its confirmations and the address and links to the (now staff-readable) order; the 409 text and `heldReason` no longer promise a moderator review. TEST: one table test in `payments_watcher_gaps_test.go` (order fresh / `updated` 48 h / address 40 d; payout blocked / pending / restored-held; sequence drop → 0 confs → recover → drop again → tip lowered; assert per-episode event and notification counts, held, sends 0 until recovery then exactly 1); `payment_review_integration_test.go` desk ordering with 1 old open flag + 101 newer; admin page has no Release form for the watcher-held row; `TestPayoutHeldWhileCreditedDepositReconfirms` still passes. Ships after A-97 (same files).
- **A-99 (MODIFY, P1; DA9-1)**. Payments and QA: a floor does not make every send succeed (fees move; requeue stays right above it) — it removes the never-sendable class; staff: flagging must set the marker the desk and staff read use and must not re-flag every pass. ACCEPTANCE: a BTC minimum payout constant (10,000 sat, documented) and an XMR minimum automatic-refund amount set on fee economics (documented); listing validation refuses a BTC price below the floor (400); `enqueuePayout` inserts no payout row when the sum is below the currency's floor and instead flags the order once (appears on the payment-review list, staff can read it, no repeat on later passes); implementation-status "Late deposits" states the exception; an above-floor payout behaves exactly as today. TEST: `TestDustBTCRefundIsFlaggedNotQueued` (fake deposit 1,000 sat, cancel, two passes → no payout, one flag note, one staff notification); inventory test: listing below floor → 400. Ships with or after A-98 (flag path).
- **A-100 (ACCEPT, P1; DA9-3)**. ACCEPTANCE: the node and wallet images in `compose.nodes.yaml` / `.env.example` default to pinned release tags matching the verified versions (bitcoind 31.1, monerod/monero-wallet-rpc 0.18.5.1), overridable in `.env`; operator-guide and implementation-status P5 state that the Monero "nothing broadcast" table holds for monero-wallet-rpc v0.18.5.1 and give a bump checklist (re-verify `moneroPreSubmitCodes` against the new wallet source before changing the tag; with an unverified version, treat every Monero send failure as possibly broadcast); CI's Compose job asserts no `:latest` for node/wallet images. TEST: the Compose configuration check; `actionlint`; docs validated.
- **A-101 (ACCEPT, P1; DA9-4, PAY9-2 folded, DA9-2, STAFF9-4)**. PAY9-2's minimum requeue delay RETRACTED by payments after reading the v0.18.5.1 source (a pool spend becomes pending on the next 20 s refresh; wallet-rpc serves one request at a time) — the runbook fix closes it more cheaply. ACCEPTANCE: `testnet-runbook.md` recovery checks use `listsinceblock <hash before the failure>` (or `listtransactions "*" 100000`) for Bitcoin and, for Monero, `refresh` then `get_transfers` twice, treating any pending/out transfer created after the failure without destinations as this payout until proven otherwise; the admin confirmation hint says the same in one line; operator-guide "Backups and recovery", the runbook reconcile section and UPGRADING §6 warn that any restore not done by `scripts/restore.sh` skips the payout protection and give the §6 SQL to run before starting the app; `restore.sh`, the §6 SQL and the step-8 clear SQL write an audit row (gate set by restore / set manually / cleared by `<admin handle>`), refusing an unknown handle. TEST: on regtest, the documented BTC command finds a payout with >50 later wallet entries (recorded log); `scripts/test-backup-restore.sh` case copying the DB with plain `pg_dump`/`pg_restore`, applying the §6 SQL, asserting the gate, holds and the audit row; `bash -n`.
- **A-102 (ACCEPT, P1; payments MISSED)**. ACCEPTANCE: saving a payout address notifies the account owner (link-free, naming the currency, not the address); A-27's suspend also holds the account's `pending` and `blocked` payouts (`held`, error prefixed `Suspended account:`) and restore does not release them automatically; an admin releases such a payout only with `confirmedTx` and a "payout address checked" confirmation, audited; the watcher never un-holds them. TEST: Go DB — vendor with a blocked release changes address → notification; admin suspends → payout held, 0 sends across passes; admin release without the confirmation → 409, with it → pending; restore leaves it held.
- **A-103 (P1, release evidence)**: run by `opsec-qa-release` at the end of this loop on the final SHA.
- **DA9-8**: the ledger's real-chain wording for A-7, A-52 and the A-13 ruling now says the logs were not retained (done in this commit).

### Iteration 10 rulings (chair, 2026-09-24) — UI & accessibility sweep (frontend, buyer, vendor, product-completeness; Rounds A–C)

No blocker survived cross-examination: BUY10-1 was revised to REQUIRED by its author after frontend showed the amount is reachable by panning. The no-JS design holds (every task reachable by keyboard in Firefox; text contrast ≥ 7:1 on all 21 preview routes except archived-card art at 3.45:1); the failures are in what the preview cannot show (real 64-hex IDs, deposits, admin payout rows, long handles) and in the recipient side of money and moderation.

- **A-115 (ACCEPT, P1, first in the UI wave)**. ACCEPTANCE: `.grid-two` tracks `minmax(0,1fr)` (mobile and desktop); `overflow-wrap:anywhere` on page-head mono text, breadcrumbs, `.account-name` and page-head h1s (or a short ID with the full ID in a wrappable block); tables scroll inside their `.table-wrap`; document scrollWidth ≤ viewport on real orders (64-hex IDs and txids) at 320, 390 and 1280 in draft, partial, paid and completed states, on /disputes and /moderator, and on /vendor with a 32-character handle at 100% and 200% text. TEST: wallet-journey width assertions after the partial deposit and after completion (390 + a 320 context); `db-orders.spec.ts` draft width; preview order uses a 64-hex ID and one sample deposit row; the 200% loop covers every preview route.
- **A-116 (ACCEPT, P1)**. ACCEPTANCE: payout resolve forms wrap inside their cell (`white-space:normal` for `.payout-actions` and `td.mono`, sensible min-width); at 1280 px with a form open every help text and checkbox label is fully visible when its control has focus; the summary names the payout, amount, recipient and order ("Resolve payout 7: refund 0.001 BTC to <handle>, order 2d6b549f"). TEST: `db-admin.spec.ts` (fresh accounts) with a seeded held/ambiguous payout: label box inside `.table-wrap` at 1280, computed white-space normal.
- **A-117 (ACCEPT, P1)**. DISSENT: buyer (keep a URL flag naming the action, accept the Back artefact) — rejected: a URL flag is replayed by Back/history under `no-store` (frontend measured it) and a crafted link can show a false success. ACCEPTANCE: a one-shot notice cookie (HttpOnly, SameSite=Strict, holding an enum code from a fixed server table, never user text) set only on the 303 of a successful signed-in POST (never on the cookieless landing path — A-48) and cleared on first render; `?saved=1` removed; specific texts ("Password changed.", "Message sent.", "Payout address saved.", "<handle> suspended.", "Signed out." …); rendered inside the targeted section or at the top without a fragment; `<title>` prefixed with the notice, and with "Error: " when `d.Error` is set; object titles for product/order/vendor pages and "Transparency"/"Marketplace" for /canary and /. TEST: `db-identity.spec.ts` save → failed password change → Back shows no success notice; reload shows it at most once; `db-admin.spec.ts` suspend → notice in viewport and title prefixed; Go unit test for the cookie round trip.
- **A-118 (ACCEPT, P1)**. ACCEPTANCE: the resolver/notification query (`actions_orders.go` ~:80/89), `NoResolver` (`orders_load.go` ~:296), dispute staff contacts (`contacts.go` ~:62) and `flagOrder`'s recipients exclude suspended accounts; with the only non-party moderator suspended the no-resolver note, admin notice and desk note appear, contacts omit the suspended account; restoring reverses it; the admin suspend copy and operator-guide no longer say orders continue normally ("orders waiting on this account stop until it is restored; the other party can still complete, cancel where allowed, or open a dispute"). TEST: `TestDisputeIgnoresSuspendedStaff` (dispute_staff_integration_test.go), a flag case in payment_review_integration_test.go, preview-auth copy assertion.
- **A-119 (ACCEPT, P1)**. ACCEPTANCE: one administrator setting "Staff contact" (free text: a handle, onion address or XMPP; password-confirmed, audited old→new) rendered on /canary, in the suspended sign-in refusal, as a "Lost your authenticator and recovery codes?" line on /challenge, under the payout-address change notice, and as "Can't read the image?" help on /login and /register; when a staff member has a key usable for encryption, a `/messages?to=<handle>` link is added; unset → "Ask the operator of this market" (no page implies an in-app channel that does not exist). TEST: Go DB — the refusal body contains the configured contact; preview-auth `/challenge` lost-factors line; setting change audited.
- **A-120 (ACCEPT, P1)**. ACCEPTANCE: `/account#payout` lists every payout to the viewer that is not sent (blocked, held, pending, sending, failed), uncapped, oldest first, with order link, amount, a per-state reason and next step in fixed copy (never raw wallet errors): blocked → save an address; failed-definite → "the market wallet refused the send before broadcasting; administrators have been notified"; failed-ambiguous → "may have been broadcast; an administrator checks the wallet before any resend"; held (watcher / restore / suspension) → neutral reason; the vendor desk shows one count linking there; `/order` replaces "No further changes are possible" with the payout's next step when an unsent payout exists for the viewer; the payout's destination is shown to its recipient only (A-84); suspension/restore notices say how many payouts are held; no order events on address save (N rows per save — PM). Folds A-107's neutral label, A-112's label and notification parts, and QA9-6/DA9-6. TEST: Go DB with blocked/held/failed payouts on orders older than 101 newer ones → all listed; recipient-only destination; wallet-journey refund leg links to `/account#payout`.
- **A-121 (MODIFY, P1)**. VEN10-1's automatic re-point on address save REJECTED (payments/buyer: a stolen password re-points; definite-failed requeue is gated only by the admin's password). ACCEPTANCE: an admin action "use the account's current address" on a held or definite-failed payout, behind `confirmedTx` and the `address_checked` confirmation, showing the payout's address and the account's current saved address (and when it last changed); ambiguous-failed, sending and restore-held payouts are never re-pointed; saving an address notifies "N unsent payouts still use your previous address; an administrator must confirm the change", naming the orders; audit records old→new. TEST: `TestAdminMovesHeldPayoutToCurrentAddress`, `TestAddressSaveDoesNotRepointUnsentPayouts`, and a pin for a suspension hold that already has an address when the owner saves a new one.
- **A-122 (MODIFY, P1)**. One shape for BUY10-2 + FE10-7: ACCEPTANCE: the complete form shows the exact release amount and recipient and carries a `required` checkbox whose label states them ("Release 0.005 BTC (test network) to <vendor> — 0.0045 more than the price. This is final."); the server refuses a missing field (400, no state change); the vendor's cancel of a paid order carries the same with the refund amount; when received > required both parties' order pages show an alert ("the buyer sent X more than required; the whole amount goes to the vendor on completion — message the vendor or open a dispute before completing"; the vendor's copy notes that cancelling refunds every deposit); the release notification names any excess; implementation-status states the rule. No automatic refund of the excess (invariant 4). TEST: Go DB missing-field 400; overpaid order page alert; wallet-journey asserts the amount in the complete label.
- **A-123 (ACCEPT, P1)**. ACCEPTANCE: the awaiting-payment page shows "Send payment before YYYY-MM-DD HH:MM UTC. If no deposit has been seen by then, the order is cancelled." computed at render with the watcher's own `paymentExpiry()` and `payment_addresses.created` (no absolute time in notifications — PM); notifications for system/moderator transitions append the event note ("Cancelled — payment window expired", "Resolved — refund to buyer"). TEST: Go DB (newPayEnv) deadline on /order; backdated expiry notification contains the reason; resolve notification contains the outcome.
- **A-124 (ACCEPT, P1)**. ACCEPTANCE: on a disputed order the party who did not open the dispute may submit one reply (20–5000 characters, same A-80 warning copy, help points to encrypted staff messages first when a staff key exists), stored once as an order event, shown to both parties and on the moderator desk with its role, rendered `white-space:pre-wrap` (also fix the reason's line breaks); a second reply → 409; not offered after resolution; the dispute-opened notification tells the non-opener the reply exists; the resolve form shows whether the other side replied. TEST: `TestDisputeCounterpartyResponse`; db-orders dispute leg with fresh accounts.
- **A-125 (ACCEPT, P1)**. ACCEPTANCE: an administrator "Hide review" action (password-confirmed, audited, reviewer notified) that hides — never deletes — a review from /product and /vendor (new column in a numbered migration); the admin sees hidden reviews labelled; implementation-status and /canary#records say administrators can hide reviews. TEST: Go DB hidden review absent from public pages; audit row; non-admin 403.
- **A-126 (ACCEPT, P1)**. ACCEPTANCE: protected-page redirects go to `/login?next=<path>` where next is accepted only if it names a registered GET page (plus its `id` query) — never an action, `//`, a scheme or an absolute URL; carried through /register and the pending sign-in through /challenge; "Sign in to see Orders." copy on /login when next is set. TEST: Go DB table test incl. open-redirect cases; `db-auth.spec.ts` anonymous checkout → sign in (and TOTP) → `/checkout?id=X`.
- **A-127 (ACCEPT, P1 docs + dead code)**. ACCEPTANCE: `/admin` runs no `orderQuery`; implementation-status and A-16 no longer claim an admin order list (building one would reverse A-24 and the /canary#records promise — a new ruling, not a doc line). TEST: existing admin tests unchanged.
- **A-128 (ACCEPT, P1)**. ACCEPTANCE: `db-seeded-marketplace.spec.ts` waits for the navigation (`waitForURL` + `waitForLoadState`) before `reload()`; grep the DB specs for the same click → `url()` → `reload()` shape (A-12 lists `db-account.spec.ts:24-25`, `db-desktop.spec.ts:44-45`) and fix them the same way; no test weakened or skipped. TEST: the specs pass 10× serially locally with fresh accounts (mind the shared-admin sign-in budget).

## P2 — high-value improvements

| ID | Item | Area | Status |
|---|---|---|---|
| A-12 | Test infra: no fail-on-skip mode for DB tests; shared Playwright admin near its sign-in budget; TOTP 30 s boundary unguarded (`db-auth.spec.ts:61,82`); click-then-reload races in `db-account.spec.ts:24-25`, `db-desktop.spec.ts:44-45` (QA3-7) and `db-seeded-marketplace.spec.ts:82-84` (turned PR #9 CI run 35998094632 red; QA9V-2: `waitForURL` + `waitForLoadState` before `reload()`). Fail-on-skip dropped (D-8) | tests | open |
| A-31 | Payment-review disposition: moderator/admin records "no action / refunded manually (txid) / escalated" as an order event + audit row (never a payout row) and the flag leaves the review list (MOD-1 remainder) | payments/staff | open |
| A-32 | Password step-up for `/resolve` (a hijacked or mis-clicking moderator queues a payout sent ~30 s later), role change, CAPTCHA toggle, operator key and canary; settings audit names old→new values (MOD-6, MOD MISSED) | identity/staff | open |
| A-33 | Display-only wallet evidence next to payout recovery forms (outgoing transfers to the address), labelled "may be incomplete after a rescan"; never gates requeue or mark-sent (MOD-2 revised; DA/PM/QA challenges) | payments | open |
| A-34 | `op=hold` for pending/blocked payouts with a required note (MOD-4 revised) | payments/staff | open |
| A-35 | Staff "needs attention" counts in navigation and at the top of /admin and /moderator (MOD-7) | ui/staff | open |
| A-36 | Provider diagnostics: Syncing / Check failing states, last poll age with stale warning, optional wallet balance vs queued payouts (MOD-8) | payments/ops | open |
| A-37 | Notifications link to their order (nullable `notifications.order_id`); vendor onboarding line on /account and operator-guide section (PM-6, PM-7) | ui | open |
| A-38 | Compose CI asserts effective config (no published ports except clearnet app on 127.0.0.1, no docker.sock) and a regtest node-profile smoke; runbook wallet dir matches compose (QA3-4, DA-5) | ops | open |
| A-39 | Browser proof of a release-to-vendor dispute with vendor and moderator pages checked after reload (QA3-5) | tests | open |
| A-40 | "Verified purchase" badge relabelled "Completed order (test network)" in UI and docs (DA-6) | commerce | open |
| A-41 | Upgrade test from an intermediate migrated commit; first tag's release notes list migrations since alpha.1 (PM MISSED) | ops | open |
| A-49 | An unfunded draft from any account blocks a listing's fulfilment-type change forever (`openOrdersSQL` counts drafts) (HUNT-C-2 = PA-3, verified) | commerce | open |
| A-50 | Listing title/description, dispute reason and moderator decision limits count bytes, forms and messages count characters (HUNT-C-3 = PA-2, verified) | commerce | open |
| A-51 | Vendor view of a buyer's draft says "Waiting for the buyer to confirm receipt." (HUNT-C-4, NIT, verified) | ui | open |
| A-55 | Locked-transfer notice tells the buyer of an already funded order to "send an ordinary transfer" (a second payment folded into the release under A-7); only for awaiting_payment (HUNT-E-4) | payments | open |
| A-56 | Payout address saved during completion leaves the payout blocked until saved again; read the recipient row `FOR SHARE` in `enqueuePayout` (HUNT-E-5, NIT) | payments | open |
| A-57 | NUL byte or invalid UTF-8 in any query or form value → 500 instead of 400/404, anonymous on catalog/product/vendor GETs (PA-1, bug hunt #1, verified) | ui | open |
| A-58 | Order and dispute pages show the listing's live title, so a vendor editing a listing during an open dispute rewrites what the buyer's order and the moderator desk say was bought; audit row has no old/new values (PA-4, verified) | commerce | open |
| A-59 | Validation errors are bare text/plain pages; the listing error covers ~14 conditions; in Firefox (Tor Browser's engine) Back restores an empty form, losing typed title/description (PA-6, verified with Playwright chromium/firefox/webkit) | ui | open |
| A-60 | NITs from bug hunt #1: catalog search treats `%`/`_` as wildcards (PA-8); dispute resubmit on a disputed order says "Draft orders contain no funds" (PA-9); mark-read of a foreign/unknown notification audits success (B-1); stale pending sign-in shows an empty challenge panel (B-4); 503-busy refusals charge the confirmation budget (B-2 remainder); /account PGP textarea maxlength 32768 vs server 16384 bytes (hunt 2 B2-3); vendor's own product page offers a checkout that always 400s (A2-3); a draft on an archived listing / demoted seller still renders "Request payment address" (A2-4); with PGP sign-in on and a saved key that stopped parsing, /account renders no password field but demands one — template and handler use different predicates; admin factor reset recovers (HUNT2-E-1 = B2-2) | ui/identity | open |
| A-63 | Migration 055's code regex is unanchored (a -38 row whose message contains " error -17: " stays definite; theoretical — v0.18.5.1 post-submit messages never do); pin A-24's staff refusal test to all 8 order actions (QA2-2, QA coverage note; NIT) | payments/tests | open |
| A-64 | (Send-claim and record-CAS pins moved into A-97.) A-52's tests do not pin the `FOR UPDATE` lock (dropping it keeps them green; safety then rests on credit-by-id + send-claim re-check); add a paused-transition case asserting `held` (QA5-1, NIT) | tests | open |
| A-65 | Signing in again from a cross-site landing page (anon fallback) leaves the previous session row live up to 12 h; delete it when the browser sent it (QA5-2, NIT; pre-existing behaviour) | identity | open |
| A-62 | A post-funding deposit is never announced when its order closes in the same watcher pass (`late_notice` set, settleOrder guard skips terminal orders); announce on terminal orders too, saying whether it was counted (QA4-1, NIT); also the order page's per-deposit **Counted** cell says "Confirmed" for an uncredited, flagged late deposit although the history says it was not paid out — show "Not counted — flagged for review" once a payout exists (hunt 2 A2-2, verified SHOULD) | payments | open |
| A-68 | Expired or signing-only PGP keys are shown to buyers as usable contact keys (no "No usable key" note) though nothing can be encrypted to them; check at display time (HUNT2-C-2, `TestHuntUnusableEncryptionKeyShownAsUsableContact`, verified) | messaging | open — folded into A-83 (ruled iteration 9; one usable-for-encryption check) |
| A-69 | Canary verification reports an expired operator key, an expired signature and a 2-minute clock skew with the forgery reason "does not verify with the configured key" (gpg says "Good signature" for two of them); expired operator key shown "Configured" (HUNT2-C-3, `TestHuntCanaryExpiryReportedAsForgery`, verified; not D-4) | transparency | open (bug-hunt 2026-09-23, unruled) |
| A-70 | Listing restore racing a vendor demotion leaves a demoted buyer with an active public listing (restore reads the role unlocked; add `FOR SHARE` as listingAction does) (HUNT2-D-2, NIT, `TestHuntDemotionRacesListingRestore`, verified) | commerce | open (bug-hunt 2026-09-23, unruled) |
| A-71 | Factor-reset picker `ORDER BY handle LIMIT 1000`, undocumented and silent: later accounts cannot be reset without SQL — fold into A-26's handle lookup (HUNT2-D-3, `TestHuntFactorResetListDropsAccountsAfter1000`, verified) | identity/staff | verified with A-26 (PR #8 @bf1ddc9, CI 35954364843) |
| A-74 | Messages stored before A-67 with plaintext in an armor header (e.g. `Comment: Ship to: …`) pass the display re-check and are shown as stored, header included, under the green badge; show `canonicalMessage(m.Body)` for rows that pass; also pin the SEIPD-only rule (tag 9 refused) and A-72's loader-level withholding with tests (QA5-1 SHOULD, QA5-2/QA5-3 NITs) | messaging/tests | verified (PR #8 @30be215, CI 35956392431; opsec-qa-release with mutation checks; each pin fails on revert; fix/a74-legacy-message-canonical a30da39) |
| A-75 | The sign-in and per-user write limiters share one 4096-entry table; after A-11 a table full of blocking sign-in entries refuses every new key, including users' write budgets, for up to 10 minutes (hard: each entry costs bcrypt behind a 4-slot gate, and CAPTCHA on by default); give write budgets their own table (A-11 implementer) | identity | open |
| A-78 | NITs from the wave-7 QA: A-9's session termination and A-28's echo restore are pinned only by manual probes (subprocess/TTY tests would catch regressions); operator guide should say a pre-reset 429 on the admin's sign-in clears after 10 minutes or an app restart; `scripts/test-upgrade.sh` leaks the PostgreSQL container's anonymous volume locally (`docker rm -f -v`) (QA8-1..3) | tests/ops | open |
| A-77 | A-8's "No eligible resolver" desk note is admin-only, but no committed test pins it (showing it to all staff keeps tests green); adopt `artifacts/qa7-30be215/qa7_a8_test.go.txt`; preview's no-background-work guard (`payments.go` Start) is also unpinned (QA7-1, NIT) | tests | open |
| A-76 | During restore reconciliation (runbook step 2) the watcher can queue a new `pending` refund for an order restored as cancelled whose refund was already sent after the backup; the gate stops the send, but no admin action or runbook SQL retires a pending payout — the runbook says to keep the gate set and get operator help (A-29 implementer) | ops/payments | open |
| A-73 | `backup.sh` reads the database name from `.env` only; if an operator exports `DATABASE_URL` in the shell, Compose uses that instead and the backup may target a different database (A-66 implementer; UNVERIFIED) | ops | open |
| A-15 | A never-confirming 0-conf deposit blocks expiry forever and keeps stock reserved | payments | open |
| A-16 | 100/50-row caps without paging: admin user picker, messages, notifications, catalog, **/orders page and admin order list** (an old shipped/awaiting order drops off /orders — HUNT-C-1, duplicate) | commerce/ui | open |
| A-22 | No browser test drives the ambiguous-requeue confirmation (Go tests only); wallet journey covers only a definite rejection. Widened (QA9-3 cut in B-round): also a moderator opening a flagged order from `/moderator#payment-review`; needs `wallet-rpc.py` `hang-next-send` (or a DB-seeded ambiguous row, since `payoutSendTimeout` has no env override — DA) and `drop` fixtures; outage/degraded leg dropped | tests | open |
| A-17 | Real-chain reorg/conflict/double-spend/locked-transfer/restart never tested; chain suite + regtest smoke not in CI | tests | open |
| A-18 | Browser coverage gaps: successful PGP proof and PGP sign-in; Firefox/WebKit only on preview pages | tests | open |
| A-104 | Staff visibility on the money side (STAFF9-3, STAFF9-5, STAFF9-7 = QA9-2, STAFF9-8): after requeue/release the payout drops off /admin (history `ORDER BY id DESC LIMIT 51` → order by `updated`) and the notice names the payout; the resolve form says "moves no funds" though resolving queues a payout sent automatically — say so and show the resulting payout state after resolving; order pages keep the deposit ledger and payout row "as last recorded" while a configured provider is unavailable (no deposit address; "until the deposit address is shown again" only on awaiting_payment); blocked payouts counted separately ("waiting for the recipient's address") | payments/staff | open |
| A-105 | A wallet that disowns an issued address (Monero `get_address_index` −2 after a restart/seed restore, a different wallet) or a Monero wallet behind its daemon is read as "no deposits": ledger rows go −1, flags raised, part-paid orders expire-cancelled silently. Count a disowned issued address as unread for the pass, treat wallet height < daemon height as syncing, clear the subaddress index cache when Check sees a different primary address (PAY9-3 revised + DA9-5 narrowed + QA9-4; full identity pinning DECLINED as over-engineered) | payments | open |
| A-106 | A first deposit to a closed order's address more than 24 h after it closed is never recorded; record and flag it (never auto-refund — DA9-1 dust / XMR fee drain), fix operator-guide:145 and runbook step 2 (PAY9-4 revised) | payments | open |
| A-107 | Payments NITs: the txid of a send whose outcome could not be recorded is lost — put txid, currency and amount in the error/log line (PAY9-5 = QA9-5); parties see "deposit conflicted" for a restore hold — neutral label (QA9-6 = DA9-6); the confirmation limiter counts successful confirmations, capping an admin at 10 payout actions per 10 min during a restore — refund the slot on success (STAFF MISSED); intake heading for an unconfigured currency; payout ID column (STAFF NITs) | payments/staff | open |
| A-108 | A-27's `FOR SHARE` in `startSession` (`actions_auth.go:~399`) is unpinned: removing it keeps the suite green, and QA's probe then left 20/30 suspended accounts with a session row that becomes valid again on restore; add a deterministic `TestSuspendDuringSignInLeavesNoSession` (QA9V-1) | tests/identity | open |
| A-109 | A suspended vendor's listings still take new drafts and payment requests (ruled "listings unchanged"); refuse new drafts and `/orders/pay` on a suspended vendor's listings with the "Unavailable. <reason>" pattern, existing orders continue (QA9V-3; ruling amendment, P2 because A-102 holds the payouts and buyers can dispute) | commerce | open |
| A-111 | Test pins from the A-79/A-82 QA: `sameProfileKey` comparing fingerprints only survives the suite — add same-key + extra UID and + third-party certification cases with `pgp_2fa` on (400, key unchanged) (QA9W-1); A-82 labels unpinned for notifications, audit, disputes, deliveries, payouts, provider polls, listing edit, payment reviews, and the `o.created` assertion is satisfied by "Last change" — anchor it and add one table-driven UTC-suffix test; loop the DSN-guard test over ~20 connections (pgx map order) (QA9W-3) | tests | open |
| A-112 | A-102 follow-ups: `restore.sh` overwrites a "Suspended account:" hold with the restore prefix, losing the address check; requeueing a failed payout for a suspended recipient has no address check; /order shows "deposit conflicted or re-confirming" for a suspension hold (fold into A-107's neutral label); the suspend notification does not say payouts are held (A-102 implementer) | payments/identity | open |
| A-113 | A-81 records copy (confirmed by QA10V; + QA10V-2 = PM10-5: the Activity log item must name message sends (not recipient), notification reads and checked payout addresses): "Temporary sign-in records … expire within 12 hours" — expired session and PGP-challenge rows stop working but are deleted only opportunistically (next sign-in, key change); say "stop working within 12 hours and are deleted later" or add them to a sweep (chair spot-check, NIT) | privacy | open |
| A-129 | UI SHOULDs from the iteration-10 sweep: sign-out moved to the top of /account (PM10-8 revised; New Identity does not end the 12 h server session); decorative glyphs and product art `aria-hidden`, `title` on `pattern=` inputs, /challenge states the 10-minute expiry, `aria-current` on staff links and Transparency, site focus ring on `pre[tabindex]`, heading levels (FE10-N1..N3); vendor "Before you sell" readiness panel (VEN10-5 revised); role-change notifications and a demoted owner may clear stored delivery content (VEN10-6); vendor listing grid uncapped for active listings with SQL counts (VEN10-7); shipped-order "payment is released when the buyer confirms" copy and `/messages?to=` filtering (VEN10-8); auto-delivery copy "every buyer receives exactly this text" (VEN10-9); pay form shows the current price and price changes are recorded; "Pay with"/"Amount" labels (BUY10-5); "Lost your authenticator?" help under the payout form (buyer MISSED); NITs BUY10-N1..N3, VEN10-N1..N3 | ui | open |
| A-130 | Double submission on slow circuits: a second tap POSTs again → text/plain 409 for a transition that succeeded, duplicate listings/messages; hidden per-form nonce, a repeat returns the original success redirect (FE MISSED, code read) | ui | open |
| A-131 | Accessibility evidence: a token-pair contrast table test (≥7:1, archived-card art documented as the exception), reduced-motion emulation, then tick acceptance.md :136, :138, :139 against that CI run (FE10-8 revised) | tests | open |
| A-132 | Image CAPTCHA on every sign-in (default) locks blind users out of existing accounts; proposal: ask for it at sign-in only after a failed password for that handle — a security trade-off deferred to the identity sweep (FE10-5; copy part in A-119) | identity | open (security ruling needed) |
| A-84 | Counterparty on-chain identifiers: the vendor sees the buyer's deposit address (while awaiting payment) and deposit/refund txids; the buyer sees the vendor's release txid (reused BTC payout address → all the vendor's payouts linkable). Hide the payout/refund txid from whoever is not its recipient and the deposit address/txids from the vendor (staff and A-24 review unchanged); payment help says Bitcoin is publicly traceable (PRIV9-4 revised by buyer/privacy B-round; BUY9-10) | privacy/payments | open |
| A-85 | Remove the XMPP field: collected under "Public profile", shown to nobody, a cross-service identifier in every backup; stop reading/saving it, new migration clears stored values (column kept) (PRIV9-7 = SEC9-8 = FE9-4 part) | privacy | open |
| A-86 | Messages: `recipientMatch` badges "to recipient's key" when the message also names other key IDs (encrypt-to-self in Kleopatra/GPG Suite/Thunderbird) — those 64-bit IDs are stored, readable by the recipient and keyserver-linkable; warn the sender and badge "+ N other key(s)" (security MISSED, `messages_status.go:35-51`); message bodies, deliveries and PGP challenges in labelled `<textarea readonly>` instead of `<pre>` so keyboard/screen-reader users can copy one message (frontend MISSED) | messaging/ui | open |
| A-87 | Identity hardening from the privacy sweep: handles unique only case-sensitively ("Alice" beside "alice", live) — refuse `lower(handle)` collisions at register/setup, non-unique index in a new migration (SEC9-5); setup-token input `type="text" autocomplete="off" spellcheck="false"` so password managers do not store the key root (SEC9-6 revised); with `COOKIE_SECURE=true` send HSTS and refuse an `http://` Origin on POST (SEC9-7) | identity/security | open |
| A-88 | Privacy copy and hints: `autocomplete="off"` on search, order-note inputs (FE9-6, measured in `moz_formhistory`); header search button readable with SVG disabled (Tor Safest) (FE9-7, measured); checkout says the vendor sees drafts and handle and cancelled drafts stay listed (BUY9-6); /messages gives one concrete `gpg --armor --encrypt --recipient <fingerprint>` example (BUY9-7); /register says there is no password reset and /totp what losing both factors means (BUY9-9); TOTP label warning about cloud-synced authenticators (PRIV9-10, NIT) | ui/privacy | open |
| A-89 | Stop writing the "Marked notification read" audit row for new events (no security value; also fixes A-60's foreign-notification success row); existing rows untouched, export byte-stable for earlier ranges (PRIV9-5 remainder) | privacy/transparency | open |
| A-90 | Operator docs: in Tor mode, local nodes connect and broadcast payouts over clearnet from the host, so peers can link market payouts to the host IP — state it and the mitigations (PRIV9-6); after a verified side-by-side restore or rollback and a fresh backup, drop the superseded database (exact `dropdb` command) (privacy MISSED, operator-guide:189) | ops/privacy | open |
| A-91 | Vendor-side gate: a physical listing cannot be published or restored while its vendor has no usable encryption key (security's A-83 dissent; deferred — changes onboarding and existing listings) | commerce | open (deferred) |
| A-92 | Handle-lookup limiter on `/messages?to=` (300 lookups in 18 s, no 429): 60 distinct lookups per 10 min in A-75's separate table (SEC9-4; deferred until A-79/A-81 disclosure lands — lower value then) | identity | open (deferred) |
| A-93 | NITs from the privacy sweep: `/static/` serves a directory listing and a build-time `Last-Modified`, static 404s lose Cache-Control (SEC9-N1); `acceptance.md:25` (no secrets in logs) has no test — add `TestLogsContainNoSecrets` capturing log output (SEC9-N2) | ops/tests | open |

## P3 — experiments

| ID | Item | Status |
|---|---|---|
| A-42 | Restore-reconciliation and staff-recovery drill on real local chains, counting shell/SQL steps per incident (PM-8, MOD-10) | open |
| A-43 | Tor-Browser-like Firefox (resistFingerprinting, XOriginPolicy=2) run of db-setup/db-auth/db-orders to measure the write path (DA-7) | open |
| A-44 | Compose bitcoind `rpcallowip` vs a 10.x Docker address pool; watcher pass time with ~200 funded orders on regtest (QA3-6, QA MISSED) | open |
| A-45 | Kill monerod mid-`transfer` on the offline stagenet fork; record the wallet-rpc error code and whether the tx reached the daemon (settles A-23 live). Extended (DA9-9 + PAY9-6 + QA9-4): time when the runbook's `get_transfers` shows a relayed-then-errored send (+0 s, +30 s, 1 block) and whether it carries destinations; rescan a seed-restored wallet while the app polls and count −1 writes; record `create_address` / `get_address_index` after a seed restore | open |
| A-20 | Legacy `service` listings still orderable; editing silently converts to physical | open |
| A-61 | Test the TOTP `otpauth://` URI (issuer encoded `+`, label `%20`) in Aegis / Google Authenticator with a spaced site name (B-5, PLAUSIBLE) | open |
| A-94 | Opt-in `MESSAGE_RETENTION_DAYS` sweep (messages and read notifications; never audit/order/payment rows; docs say backups keep older copies) (PRIV9-8; DEFER until A-81 discloses today's behaviour) | open (deferred) |
| A-95 | Nodes over Tor: bitcoind `-proxy`/`-onlynet=onion` or `-privatebroadcast` (does it cover `sendtoaddress`?), monerod `--tx-proxy`; 5 payouts each on signet/stagenet, record sync, broadcast success and any clearnet connection (PRIV9-9; settles whether A-90 becomes a Compose change) | open |
| A-96 | 3–5 people new to PGP, Tor Browser scripts off, paid physical order: can they send the address encrypted within 15 min, and how many type it into a readable field? (BUY9-11). A-43 evidence update (FE9): a Safest-like Firefox (JS/SVG/MathML off, resistFingerprinting, XOriginPolicy=2) completed setup, CAPTCHA, role change, PGP key + proof, listing, message and draft against a real DB with zero cross-origin requests; real Tor Browser over an onion address still outstanding | open |
| A-133 | Tor Browser (Safest) with NVDA / VoiceOver / TalkBack: is the page exposed to screen readers and are load-time notices/titles spoken? (FE10-9; vendor/buyer usability studies fold into A-96 — BUY10-8, VEN10-E1) | open |
| A-46 | Publish a prerelease tag once P0/P1 close; UPGRADING retitled from→to; verification.md cites the tag's CI run (PM-5) | blocked (owner: release decision) |

## Declined

| ID | Item | Why |
|---|---|---|
| D-1 | Admin "mark sent" being the only exit for some held payouts | conservative by design (Fable ruling) |
| D-2 | Tor onion reachability as a roadmap item | environment evidence, not code; track under ops verification |
| D-3 | A-14 auto-completion/abandonment timers | first unattended payout trigger, against the human-confirmed payout posture; abandonment already has a vendor-dispute path (`orders_state.go`) (DA-8, iteration 3) |
| D-4 | A-19 canary freshness indicator | page already shows signature and posted dates; freshness needs an operator policy (DA-10) |
| D-5 | Web unlock of `payments_recovery_required` | clearing it is an operator assertion after reconciliation; a button makes a false claim cheap (DA-11, QA3, MOD) |
| D-6 | Web "record an external payout" and mark-sent refusing unknown txids | a hijacked admin session could suppress a real payout (unique `payouts.order_id`); wallets restored from seed do not know later txids (PM-4 original, MOD-2 original; iteration 3) |
| D-7 | Global web payout pause | duplicates the restore gate and stopping the app; `op=hold` (A-34) covers the real case (MOD-4 revised) |
| D-9 | Per-party message delete (BUY9-8) | one shared row: deleting erases the counterparty's copy (dispute evidence), hiding leaves the ciphertext in the database and backups — a button promising an erasure that does not happen; security, privacy and frontend all rejected it; A-94 is the only shape that removes rows (iteration 9) |
| D-10 | Confirm checkbox for key user IDs that look like names/emails (SEC9-1 original) | withdrawn by security: fires on nearly every key, heuristic, no-JS round-trip loses the pasted key; A-79 disclosure instead (iteration 9) |
| D-11 | User account/data deletion ("erase me") | orders, payouts and audit rows underpin invariants 3, 4 and 8, and `auditExport` joins `users.handle`, so deleting/renaming changes past export bytes; A-81 disclosure is the proportionate answer for a test-network market (buyer, privacy NOT WORTH; iteration 9) |
| D-12 | Minimum age before requeueing an ambiguous payout (PAY9-2 original) | retracted by payments after the v0.18.5.1 source: a pool spend becomes pending on the next 20 s auto-refresh and wallet-rpc serves one request at a time; A-101's runbook commands close the gap (iteration 9) |
| D-13 | Full wallet/chain identity pinning (Monero primary address stored + Bitcoin `getaddressinfo ismine` + provider match) (PAY9-3 original) | over-engineered (payments itself, QA): Monero misattribution needs an in-process wallet swap; a recreated Bitcoin wallet draws fresh keys and fails safe; the real harm (silent expiry) is A-105 (iteration 9) |
| D-14 | Browser proof of every payment recovery journey with hang/drop/check-fail fixtures (QA9-3 original) | Go DB pins the server refusals; a hang case costs >30 s per case or a test knob in production (DA); cut to A-22 widened (staff, DA, QA) (iteration 9) |
| D-15 | Automatic re-point of held/failed payouts on address save (VEN10-1 original) | a stolen password re-points; definite-failed requeue is gated only by the admin's password — replaced by the admin action A-121 (iteration 10) |
| D-16 | Sign-out form in the header (PM10-8 original) | header already 529–707 px tall at 320 px/200% and an accidental POST one Tab stop after the skip link; moved to the top of /account (A-129) (iteration 10) |
| D-17 | Single-currency listings (VEN10-5 part), full-page contrast node walk in CI (FE10-8 original), per-role reachability crawl (PM10-11), per-unit delivery key pool (VEN10-9) | each withdrawn by its author as costing more than it prevents (iteration 10) |
| D-18 | Notice as a URL code (`?saved=<code>`) (buyer's A-117 alternative) | replayed by Back under no-store and forgeable by a crafted link (iteration 10) |
| D-8 | Admin password reset for users; fail-on-skip mode for DB tests | reset is a takeover path with no email; CI already fails on an unreachable DB URL (PM NOT WORTH, DA-9) |

## Closed

| ID | Item | Evidence |
|---|---|---|
| C-4 | QA-1: a session revoked by a concurrent password change could still change the password | `actions_account.go` own-session check; `TestPasswordChangeConcurrentRevokedSession` fails without the fix |
| C-5 | QA-2/QA-3: rotation docs pointed to SQL instead of the admin reset; restore accepted system databases | operator-guide.md, UPGRADING.md; `restore.sh` guard + `test-internal-db-restore.sh` cases |
| C-1 | CI red on main: `pg_isready` Unix-socket init race | PR #5, main CI run 35891255332 green |
| C-2 | Go toolchain drift (tests 1.26.8, ships 1.27.1); govulncheck 1.1.4 panics on Go 1.27 | PR #5 |
| C-3 | Release workflow required a private repo; action pin drift; duplicate CI runs | PR #5 |
